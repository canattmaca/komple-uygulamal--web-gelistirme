let sonuc;
let a=10, b=30, c=10;

 
// Aritmatik Oparatör

        // sonuc=a+b;
        // sonuc=a-b;
        // sonuc=a*b;
        // sonuc=a/b;
        // sonuc=a%b;
        // sonuc=a++ ; a+1
        // sonuc=++a;
        // sonuc=a--;
        // sonuc=a;

// Atama Oparatör
            // sonuc=a;
            // sonuc += a// sonuc=a+sonuc
            // sonuc += (a+b)// sonuc=sonuc + a + b
            // sonuc -= a// sonuc=a-sonuc
            // sonuc *= a// sonuc=a*sonuc
            // sonuc /= a// sonuc=a/sonuc
            // sonuc %= a// sonuc=a%sonuc


// Karşılaştırma Oparatör
        // sonuc=(a==b)
        // sonuc=(a!=b) //eşit dğeil mi?
            sonuc=(3==="3") //eşit dğeil mi?  İlk eşitlikte içibdeki değere bakar ama 3 eşittir durumunda type kontrolu yapar ve false çeker.
       sonuc=( a>b)
       sonuc=( a<b)
       sonuc=( a>=b)
       sonuc=( a<=b)


// Mantıksal Oparatör

console.log(sonuc);

